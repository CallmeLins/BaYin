{
  "name": "bayin-workspace",
  "private": true,
  "version": "0.1.0",
  "description": "Workspace scripts for the BaYin Tauri application",
  "scripts": {
    "dev": "npm run dev --prefix src-ui",
    "build": "npm run build --prefix src-ui",
    "tauri": "tauri",
    "tauri:dev": "tauri dev",
    "tauri:build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.0"
  }
}
