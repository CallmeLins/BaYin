import 'package:flutter/widgets.dart';

import '../../widgets/widgets.dart';

/// Stub — real implementation lands in Phase 7.
class UpdateSoftwarePage extends StatelessWidget {
  const UpdateSoftwarePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const PlaceholderPage(title: 'Update software', phase: 'Phase 7');
  }
}
