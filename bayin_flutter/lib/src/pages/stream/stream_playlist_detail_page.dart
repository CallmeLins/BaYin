import 'package:flutter/widgets.dart';

import '../../widgets/widgets.dart';

/// Stub — real implementation lands in Phase 6 (stream playlists via Subsonic/
/// Jellyfin/Emby).
class StreamPlaylistDetailPage extends StatelessWidget {
  const StreamPlaylistDetailPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const PlaceholderPage(
      title: 'Stream playlist detail',
      phase: 'Phase 6',
    );
  }
}
